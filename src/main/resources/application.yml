server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/healthcheck
    username: hc_user
    password: hc_pass123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ton-email@gmail.com
    password: ton-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: hpe-healthcheck-secret-key-2024
  expiration: 86400000
